<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <groupId>org.mortbay.jetty</groupId>
    <artifactId>modules-extra</artifactId>
    <version>7.0.0pre0</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>jetty-runner</artifactId>
  <name>Jetty Extra :: Web Runner</name>
  <url>http://jetty.mortbay.org</url>
  <build>
    <defaultGoal>install</defaultGoal>
    <plugins>
     <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>unpack-dependencies</goal>
            </goals>
            <configuration>
              <includes>**</includes>
              <excludes>**/MANIFEST.MF</excludes>
              <outputDirectory>${project.build.directory}/unpacked-dependencies</outputDirectory>
              <overWriteReleases>false</overWriteReleases>
              <overWriteSnapshots>true</overWriteSnapshots>
            </configuration>
          </execution>
        </executions>
      </plugin>


      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>runner jar</id>
            <phase>install</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <jar jarfile="${project.build.directory}/jetty-runner.jar">
                  <fileset dir="${project.build.directory}/classes" />
                  <fileset dir="${project.build.directory}/unpacked-dependencies" />
                  <manifest>
                    <attribute name="mode" value="development" />
                    <attribute name="Main-Class" value="org.mortbay.jetty.runner.Runner" />
                    <attribute name="Built-By" value="${user.name}" />
                    <attribute name="implementation-version" value="${project.version}" />
                    <attribute name="package" value="org.mortbay.jetty.runner" />
                    <attribute name="url" value="http://jetty.mortbay.org" />
                    <attribute name="Bundle-License" value="http://www.apache.org/licenses/LICENSE-2.0" />
                    <attribute name="Bundle-Name" value="Jetty Runner" />
                    <attribute name="Bundle-Vendor" value="Mort Bay Consulting" />
                  </manifest>
                </jar>
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty-plus</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty-jndi</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jsp-2.1</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>
</project>
